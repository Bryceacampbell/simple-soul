import React from "react";
import NasmImg from "../../images/nasm.png";
const NASM = () => {
  return (
    <img
      width="5rem"
      style={{ position: "absolute", bottom: "2rem", right: "2rem" }}
      src={NasmImg}
      alt="Nasm Logo"
    />
  );
};

export default NASM;
